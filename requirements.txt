sounddevice>=0.4.6
soundfile>=0.12.1
numpy>=1.24.0
keyboard>=0.13.5
mouse>=0.7.1
pynput>=1.7.0
pillow>=10.0.0
pydub>=0.25.1
imageio-ffmpeg>=0.6.0  # Bundled ffmpeg for pydub OGG/M4A/AAC support
librosa>=0.10.0  # Time-stretching for pitch-preserving speed changes
scipy>=1.10.0  # Biquad bandpass for the Voice Changer radio/megaphone effect (also a librosa dep)
customtkinter>=5.2.0  # Modern UI widgets with rounded corners
windnd>=0.0.7  # Windows drag-and-drop support for file imports
emoji-data-python>=1.6.0  # Emoji data with categories for emoji picker
colour>=0.1.5  # Color manipulation and utilities
pyrnnoise>=0.4.3  # Only for its bundled rnnoise.dll: audio.py binds it directly via ctypes (the package's own
                  # import chain audiolab/av is NOT used and is excluded from the EXE). Light NS engine.
pystray>=0.19.5  # System tray icon (minimize-to-tray)
python-bidi>=0.4.2  # Unicode Bidirectional Algorithm for Hebrew/Arabic text display
yt-dlp>=2026.8.19  # YouTube/audio downloader for the ⬇ Web button. KEEP THIS FRESH:
                   # YouTube breaks older releases ("This video is not available").
                   # `pip install -U yt-dlp` then re-deploy — the EXE bundles it.
static-ffmpeg>=2.5  # Bundles ffmpeg + ffprobe for yt-dlp's MP3 postprocessor
soundcard>=0.4.6  # WASAPI loopback recording (captures other people on Discord calls)
qrcode>=7.4  # QR code for the 📱 Send to Phone dialog (mobile companion sync)
